<Window x:Class="Wpf.ActivityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf"
        mc:Ignorable="d"
        Title="Tätigkeiten bearbeiten" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBlock>Tätigkeiten des Mitarbeiters</TextBlock>
            <ComboBox ItemsSource="{Binding Employees}" SelectedItem="{Binding SelectedEmp}"></ComboBox>
        </StackPanel>
        <ListView  Grid.Row="1" ItemsSource="{Binding Activities}">
            <ListView.View>
                <GridView >
                    <GridViewColumn Header="Datum" DisplayMemberBinding="{Binding Date, StringFormat=dd.MM.yyyy}"></GridViewColumn>
                    <GridViewColumn Header="Von" DisplayMemberBinding="{Binding StartTime, StringFormat=HH:mm}"></GridViewColumn>
                    <GridViewColumn Header="Bis" DisplayMemberBinding="{Binding EndTime, StringFormat=HH:mm}"></GridViewColumn>
                    <GridViewColumn Header="Tätigkeit" DisplayMemberBinding="{Binding ActivityText}"></GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
